extends layout

block content
  h1= title
  - var image_names = ["TeslaM3", "Highlander", "Explo", "C1", "C2","C3"]
  - for(let i = 0; i < 3; i++) {
    - let number = Math.floor(Math.random() * 5)
        img(src=`/images/${image_names[number]}.jpg`, height="380px", width="380px")
  - }


